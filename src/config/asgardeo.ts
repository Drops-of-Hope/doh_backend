/**
 * Asgardeo Identity Provider Configuration
 * 
 * This file contains specific configuration for Asgardeo Identity and Access Management.
 * It manages:
 * - Asgardeo OAuth 2.0 client configuration
 * - OIDC discovery endpoint setup
 * - Token endpoint and authorization server details
 * - User info endpoint and profile mapping
 * - Logout endpoint and session management
 * - Scope definitions for different access levels
 * 
 * OAuth 2.0 Configuration:
 * - Client ID and secret management
 * - Redirect URI configuration for callbacks
 * - Supported grant types (authorization_code, refresh_token)
 * - Token response type and format specifications
 * - PKCE (Proof Key for Code Exchange) configuration
 * 
 * OIDC Features:
 * - OpenID Connect discovery document parsing
 * - User profile attribute mapping
 * - Custom claim extraction and processing
 * - Role and group membership handling
 * - Multi-tenant organization support
 * 
 * Integration aspects:
 * - Frontend authentication flow coordination
 * - Mobile app authentication support
 * - API access token validation
 * - Refresh token rotation policies
 * - Single sign-on (SSO) capabilities
 */
